rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for all collections and subcollections so user trips, finances,
    // profiles, settings, dropdowns, and auth sync work seamlessly
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

